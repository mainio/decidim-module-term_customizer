# frozen_string_literal: true

require "spec_helper"

describe Decidim::TermCustomizer::Engine do
  it "sets up term customizer during initialization" do
    one = 1
    expect(one).to eq(1)
  end
end
